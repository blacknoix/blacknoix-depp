# TEMPLATE ONLY — not listed in kustomization.yaml.
#
# Requires: External Secrets Operator installed + approved ClusterSecretStore
# or SecretStore referencing AWS Secrets Manager (no ARNs/account IDs here).
#
# API group/version must match the installed ESO release. Confirm before activate.
# Do not commit secret values, account IDs, or ARNs.
#
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: api-gateway-runtime
#   namespace: depp-staging
#   labels:
#     app.kubernetes.io/name: api-gateway
#     depp.blacknoix.io/environment: staging
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: REPLACE_WITH_APPROVED_SECRETSTORE_NAME
#     kind: SecretStore
#   target:
#     name: api-gateway-runtime
#     creationPolicy: Owner
#   data:
#     - secretKey: DATABASE_URL
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: DATABASE_URL
#     - secretKey: AUTH_MODE
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: AUTH_MODE
#     - secretKey: AUTH_EXPLICIT_ROLES_MODE
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: AUTH_EXPLICIT_ROLES_MODE
#     - secretKey: JWT_ACCESS_SECRET
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: JWT_ACCESS_SECRET
#     - secretKey: JWT_ISSUER
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: JWT_ISSUER
#     - secretKey: JWT_AUDIENCE
#       remoteRef:
#         key: REPLACE_WITH_APPROVED_SECRETS_MANAGER_NAME
#         property: JWT_AUDIENCE
#     # Optional when OIDC_ISSUER is enabled (all required together):
#     # OIDC_ISSUER, OIDC_CLIENT_ID, OIDC_CLIENT_SECRET, OIDC_REDIRECT_URI, OIDC_TENANT_ID
#
# DATABASE_MIGRATION_URL must NOT be injected into the api-gateway runtime Secret.
# Migrations use a separate Job/identity (depp_migrator) in a later slice.
