apiVersion: v1
kind: ServiceAccount
metadata:
  name: api-gateway
  labels:
    app.kubernetes.io/name: api-gateway
    app.kubernetes.io/part-of: depp
    depp.blacknoix.io/environment: staging
# No Kubernetes API access required for api-gateway today — no Role/RoleBinding.
# Future IRSA/workload identity may annotate this SA; do not invent ARNs here.
automountServiceAccountToken: false
